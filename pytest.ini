[pytest]
# The gateway imports `models` from the repo root, and uvicorn is launched from
# there too — put the repo root on sys.path so tests match runtime import paths.
pythonpath = .
testpaths = tests
markers =
    live: end-to-end checks against real nodes + gateway on :8000-:8003 (auto-skipped if not running)
